# Domain Map

Status: Draft
Version: 1.9.0

See: ../glossary.md
[[glossary]]

## Purpose

Defines the top-level Apple development domains used to organize References, Knowledge Contracts, Skills, and Workflows, and the Tier (build-order priority, see glossary above) assigned to each.

## Build Order

One domain is fully finished (Reference → Knowledge → Skill → Validation) before the next domain starts. Domains are attempted in Tier order: all of Tier 1, then Tier 2, then Tier 3. Within a tier, order is chosen at build time.

Tiers ranked by real-world usage frequency (2026-07-31 re-rank, cross-checked against the full Apple Developer Documentation technology index): Tier 1 = needed by nearly every iOS app, Tier 2 = common but not universal, Tier 3 = vertical/niche.

Completed: `style-guide` (Tier 1), `authentication` (Existing/Unscheduled — cross-cutting, built ahead of tier order per Phase 5 decision), `human-interface-guidelines` (Tier 1 — Foundations v1 (15 KCs) plus a curated Patterns/Components/Inputs v1 (18 KCs): Patterns (onboarding, searching, settings, notifications, feedback, undo/redo), Components (lists and tables, buttons, sheets, alerts, action sheets, navigation bars, tab bars, pickers, toggles, text fields, menus), and one Inputs topic (touchscreen gestures); remaining Patterns/Components/Inputs topics (e.g. Charts, Drag and Drop, Multitasking, Column Views, Sliders, Toolbars beyond navigation, Apple Pencil, Game Controllers) remain unbuilt), `app-store-review-guidelines` (Tier 1 — critical-subset v1: 2.1, 2.3, 3.1.1, 4.2, 4.3, 5.1.1, 5.1.2; Safety, most of Legal, Design 4.0, and Guideline 4.8 remain unbuilt), `swiftui` (Tier 1 — Views/Navigation/Layout/State v1 plus Animation/Gestures v1, iOS 17+ conventions: implicit/explicit animation, timing curves, transitions, matchedGeometryEffect, the Animatable protocol, PhaseAnimator/KeyframeAnimator, tap/long-press gestures, drag gesture, magnification/rotation gestures, gesture composition, GestureState; previews, custom Layout protocol conformances, and legacy ObservableObject/NavigationView migration guidance remain unbuilt), `accessibility` (Tier 1 — SwiftUI + UIKit accessibility API v1: labeling, traits, value/hint, custom actions, element grouping, navigation order, Dynamic Type API, reduce-motion/transparency/increase-contrast, keyboard access & focus, hidden/decorative elements, accessibility audits), `uikit` (Tier 1 — programmatic screen-scaffolding v1: view controller lifecycle/composition, Auto Layout, navigation, diffable table/collection views, modal presentation; Storyboard/XIB, gesture recognizers, Core Animation, custom transitions, and SwiftUI interop remain unbuilt), `sf-symbols` (Tier 1 — core rendering/variants v1 across SwiftUI + UIKit: symbol basics, rendering modes, symbol variants, variable value, weight/scale, color/tinting, custom symbol usage, UIKit SymbolConfiguration; symbol effects/animations and Symbol Composer authoring remain unbuilt), `networking` (Tier 1 — async/await URLSession v1: request construction, data fetching, Codable decoding, HTTP error handling, task cancellation, session configuration, App Transport Security, authenticated requests; completion-handler APIs, Combine, and URLSessionDelegate-based background/progress/TLS handling remain unbuilt), `xcode` (Tier 1 — Xcode GUI/project-file v1: build configurations & xcconfig files, schemes/targets, automatic and manual code signing, entitlements/capabilities, archive process, export options; xcodebuild CLI, CI signing automation, and Swift Package Manager build configuration remain unbuilt), `local-authentication` (Tier 1 — iOS/iPadOS LocalAuthentication framework API v1: availability/biometry-type detection, policy evaluation, reason strings & NSFaceIDUsageDescription, LAError handling, LAContext lifecycle, Keychain-biometric binding, fallback UX; macOS/watchOS-specific behavior and general Keychain storage remain unbuilt), `app-tracking-transparency` (Tier 1 — iOS/iPadOS AppTrackingTransparency + AdSupport framework API v1: authorization-request mechanics, authorization status handling, IDFA access, NSUserTrackingUsageDescription; tvOS-specific behavior, SKAdNetwork, and AdServices attribution remain unbuilt). **All 11 Tier 1 domains complete** (12 domains completed total, including `authentication` cross-cutting/unscheduled); `human-interface-guidelines` and `swiftui` each expanded post-completion — `human-interface-guidelines` to add its Patterns/Components v1, `swiftui` to add its Animation/Gestures v1. **Tier 2 begun**: `usernotifications` (Tier 2 — client-side UserNotifications + UIKit push-registration API v1: authorization request, local notification scheduling, remote push registration, delegate handling (foreground presentation, response handling), actions/categories, and pending/delivered/badge management; rich-media extensions, critical alerts, location-based triggers, APNs server-side payload construction, ActivityKit/Live Activities, and watchOS-specific interfaces remain unbuilt) — first Tier 2 domain complete, picked as the highest real-world-usage domain in the tier. `privacy` (Tier 2 — `PrivacyInfo.xcprivacy` privacy manifest file implementation v1: manifest structure/bundling, required-reason API declarations, collected data type declarations, tracking domains and third-party SDK signature requirement; App Store Connect nutrition-label questionnaire, permission-request UI design, Info.plist usage strings, and Keychain/security remain out of scope, owned elsewhere) — second Tier 2 domain complete. `foundation` (Tier 2 — curated, highest-usage v1 subset of Swift Foundation: date/time formatting, measurement/unit formatting, Codable encoding and custom conformance, FileManager app sandbox directories; general String/Unicode processing, `NotificationCenter`, `Result`, GCD/`DispatchQueue`, Locale/Bundle localization, Combine, and network-response Codable decoding remain out of scope, owned elsewhere or deferred to future domains) — third Tier 2 domain complete. `security` (Tier 2 — general (non-biometric-bound) Keychain Services API v1: Keychain item CRUD (`SecItemAdd`/`SecItemCopyMatching`/`SecItemUpdate`/`SecItemDelete` for `kSecClassGenericPassword`/`kSecClassInternetPassword`, `OSStatus`/`errSec*` handling), Keychain accessibility levels (`kSecAttrAccessible*` family), Keychain access groups and sharing (`kSecAttrAccessGroup`, Keychain Sharing capability, team-ID prefix convention), storing structured/`Codable` data (`kSecValueData`); biometric-bound access control (`SecAccessControl`), AutoFill/credential-provider extensions, iCloud Keychain sync, and non-password item classes (certificates, keys, identities) remain out of scope, owned elsewhere or deferred) — fourth Tier 2 domain complete. `storekit` (Tier 2 — StoreKit 2 async/await API v1: product loading and purchase initiation (`Product.products(for:)`, `product.purchase()`, `Product.PurchaseResult`), transaction verification and entitlements (`VerificationResult`, `Transaction.currentEntitlements`, `transaction.finish()`), transaction updates and restoring purchases (`Transaction.updates` listener, `AppStore.sync()`), and subscription status and renewal info (`Product.SubscriptionInfo.Status`/`RenewalInfo`/`renewalState`, subscription-group upgrade/downgrade); legacy StoreKit 1 (`SKPaymentQueue`), server-side receipt validation/App Store Server API, and StoreKit Configuration file test setup remain out of scope — App Review compliance angle owned by `app-store-review-guidelines`) — fifth Tier 2 domain complete. `authenticationservices` (Tier 2 — Sign in with Apple API implementation v1: building the authorization request and reading the resulting credential (`ASAuthorizationAppleIDProvider().createRequest()`, `requestedScopes`, `ASAuthorizationController`, `ASAuthorizationControllerDelegate`/`ASAuthorizationControllerPresentationContextProviding`, `ASAuthorizationAppleIDCredential`, `ASAuthorizationError`), nonce generation and identity-token/authorization-code handling (SHA256-hashed nonce, forwarding raw nonce/`identityToken`/`authorizationCode` to a backend), credential-state checks and revocation (`getCredentialState(forUserID:)`, `CredentialState`, `credentialRevokedNotification`), and session persistence and sign-out (the `user` identifier as durable account key); Password AutoFill/credential-provider extensions, Passkeys/WebAuthn APIs, and server-side JWT verification remain out of scope — sign-in UX/terminology owned by `authentication`, Keychain CRUD owned by `security`) — sixth Tier 2 domain complete, and resolves the `authentication`/`authenticationservices`/`sign-in-with-apple` three-way boundary this document had left unresolved (see Cross-Domain Notes). `widgetkit` (Tier 2 — WidgetKit framework API v1: widget declaration and families (conforming to `Widget`/`WidgetConfiguration` — `StaticConfiguration`/`AppIntentConfiguration`, `WidgetBundle`, `supportedFamilies`/`widgetFamily`, `containerBackground(for:)`, `kind`-string stability), timeline provider and entries (`TimelineProvider`'s `placeholder(in:)`/`getSnapshot(in:completion:)`/`getTimeline(in:completion:)`, `TimelineEntry`, `Timeline`, `TimelineReloadPolicy`), widget interactivity and deep links (`widgetURL(_:)`, `Link`, `Button(intent:)`/`Toggle(_:isOn:intent:)` wiring an already-authored `AppIntent`), and timeline reloading and refresh budget (`WidgetCenter.reloadTimelines`/`reloadAllTimelines`, the best-effort system-managed refresh budget); Live Activities/`ActivityKit`, watchOS complications as a distinct surface, Control Widgets (iOS 18 `Controls`/`ControlWidget`), StandBy-specific layout, `AppIntent` authoring itself (deferred to the future `app-intents` domain), and background-refresh scheduling mechanics (deferred to the future `backgroundtasks` domain) remain out of scope — seventh Tier 2 domain complete. `app-intents` (Tier 2 — App Intents framework API v1: app intent declaration and parameters (`AppIntent` conformance — `title`/`description`/`perform()`, the `@Parameter` property wrapper documented under the symbol `IntentParameter`, `AppEnum`-conforming custom enums, `parameterSummary`), app entities and queries (`AppEntity` conformance, `EntityQuery`/`EntityStringQuery`, `DisplayRepresentation`, `defaultQuery`, `suggestedEntities()`), App Shortcuts and Siri phrases (`AppShortcutsProvider`'s `appShortcuts`, the `AppShortcut` struct, `\(.applicationName)` phrase-authoring rules, `updateAppShortcutParameters()`), and intent results and widget hookup (`IntentResult`/`ReturnsValue`/`ProvidesDialog`/`OpensIntent`) — resolves the seam `widgetkit` had proactively deferred (authoring the `AppIntent` itself); legacy `SiriKit` donation-based intents, custom Siri vocabulary beyond basic phrase authoring, Interactive Snippets, Spotlight indexing of entities, `LiveActivityIntent` specifics, and wiring an already-authored intent into a widget (owned by `widgetkit`) remain out of scope — eighth Tier 2 domain complete. `backgroundtasks` (Tier 2 — BackgroundTasks framework API v1: background task registration and scheduling (`BGTaskScheduler.shared.register(forTaskWithIdentifier:using:launchHandler:)`, `BGTaskSchedulerPermittedIdentifiers`, `BGAppRefreshTaskRequest`/`BGProcessingTaskRequest`, `submit(_:)`, `earliestBeginDate`), task execution and expiration handling (`BGTask`/`BGAppRefreshTask`/`BGProcessingTask`, `expirationHandler`, `setTaskCompleted(success:)`, re-submission for recurrence), processing task constraints and conditions (`requiresNetworkConnectivity`/`requiresExternalPower`, choosing `BGProcessingTaskRequest` over `BGAppRefreshTaskRequest`), and background refresh and widget timeline hookup — resolves the second seam `widgetkit` had proactively deferred (background-refresh scheduling mechanics); `BGContinuedProcessingTask`, legacy Background Fetch, unrelated background modes (audio/location/VoIP), and `URLSession` background transfer (owned by `networking`) remain out of scope — ninth Tier 2 domain complete. `eventkit` (Tier 2 — EventKit framework API v1: authorization and access levels (`EKAuthorizationStatus`, the iOS 17+ split between `requestFullAccessToEvents(completion:)`/`requestWriteOnlyAccessToEvents(completion:)`/`requestFullAccessToReminders(completion:)` and the legacy `requestAccess(to:completion:)`, matching Info.plist usage-description keys), event CRUD and fetch predicates (`EKEvent`, `save(_:span:commit:)`/`remove(_:span:commit:)`, `predicateForEvents(withStart:end:calendars:)` + `events(matching:)`), reminder CRUD and fetch (`EKReminder`, `save(_:commit:)`/`remove(_:commit:)`, `predicateForReminders(in:)` + async-only `fetchReminders(matching:completion:)`), and recurrence rules and the EventKitUI hand-off (`EKRecurrenceRule`/`EKRecurrenceEnd`, choosing `EKEventEditViewController`/`EKEventViewController` over custom UI); `EKSource`/multi-account calendar-source management as its own topic, CalDAV/Exchange specifics, `EKEventStoreChanged` live-sync notifications, and EventKit inside a widget extension (owned by `widgetkit`) remain out of scope — tenth Tier 2 domain complete. `tipkit` (Tier 2 — TipKit framework API v1: tip declaration and content (`Tip` protocol conformance on a `struct` — `title`, optional `message`/`image`, `actions` via `Tip.Action`), display rules and event triggers (the `#Rule(_:)` macro over `Tips.Parameter`-wrapped state or a donated `Tips.Event`, AND-combination of every rule), tip options and app configuration (`Tips.configure(_:)` at app launch — `datastoreLocation(_:)`/`displayFrequency(_:)` — and per-tip `Tip.Option`s `MaxDisplayCount`/`MaxDisplayDuration`/`IgnoresDisplayFrequency`), and presenting tips and tip groups (`TipView`/`popoverTip(_:arrowEdge:action:)` vs. UIKit's `TipUIView`/`TipUIPopoverViewController`, `TipGroup` priority (`.firstAvailable` default vs. `.ordered`), `invalidate(reason:)`); custom `TipViewStyle` authoring beyond the system default, watchOS-specific presentation differences, and `Tips.ConfigurationOption.cloudKitContainer(_:)` cross-device datastore sync (a real, documented capability, deliberately excluded rather than assumed nonexistent) remain out of scope — eleventh Tier 2 domain complete. `passkit` (Tier 2 — PassKit framework API v1, Wallet passes and Apple Pay both included: pass library querying/adding (`pass-library-and-authorization`) — `PKPassLibrary.isPassLibraryAvailable()`/`containsPass(_:)`/`passes()`/`passes(of:)`/`addPasses(_:withCompletionHandler:)`, and why no EventKit-style permission gate exists; `.pkpass`/`pass.json` structure (`pass-content-and-required-fields`) — required top-level keys, the five style keys, `PassFields` groups, current `barcodes`/`relevantDates` vs. deprecated singular `barcode`/`relevantDate`, client-side `PKPass(data:)`; the add-to-Wallet UI flow (`adding-passes-ui`) — `PKAddPassesViewController`/`PKAddPassButton`, `canAddPasses()`, UIKit-only with no SwiftUI representable; pass updates and the app-vs-server push boundary (`pass-updates-and-push-registration`) — `webServiceURL`/`authenticationToken`, the device-registration/update REST protocol, and the corrected finding that no PassKit-specific `PKPushType` exists (the system Wallet component handles push tokens, not the app); Apple Pay payment requests (`apple-pay-payment-request`) — `PKPaymentRequest`, `canMakePayments()`/`canMakePayments(usingNetworks:)`, `PayWithApplePayButton` vs. `PKPaymentButton`, `PKPaymentAuthorizationController` vs. `PKPaymentAuthorizationViewController` (both current, neither deprecated); and Apple Pay authorization and result handling (`apple-pay-authorization-and-result-handling`) — `PKPaymentAuthorizationControllerDelegate`, `PKPayment`/`PKPaymentToken` (encrypted, decryption server-side), `PKPaymentAuthorizationResult`, `paymentAuthorizationControllerDidFinish(_:)`; server-side pass signing/certificate/Pass Type ID setup, `PKAddSecureElementPassViewController`/NFC/secure-element passes, `PKPassPersonalization`, and Apple Pay server-side merchant validation/token decryption/payment-processor integration remain out of scope — twelfth Tier 2 domain complete. `swiftdata` (Tier 2 — SwiftData framework API v1, modern SwiftUI-native persistence; Core Data remains a separate, not-yet-built domain, see Cross-Domain Notes: model definition (`model-definition`) — `@Model` on a class only, `@Attribute`/`@Relationship`/`@Transient`, and the corrected finding that `@Model` synthesizes `PersistentModel`/`Observable` (which cover `Hashable`/`Identifiable`) but never `Codable`; container setup (`model-container-setup`) — `.modelContainer(for:)`/`.modelContainer(_:)`, `ModelConfiguration`, manual `ModelContainer(for:configurations:)`, `@Environment(\.modelContext)`, and the corrected finding that the modifier's `inMemory:` parameter and `ModelConfiguration`'s `isStoredInMemoryOnly` are two distinct names at two layers, not one symbol; context CRUD (`model-context-crud`) — `insert(_:)`/`delete(_:)`/`save()`, `autosaveEnabled` (true by default only for `mainContext`, false otherwise), `mainContext` vs. a manual secondary `ModelContext`, `undoManager`; querying (`querying-with-query-and-fetchdescriptor`) — `@Query`(`filter:`/`sort:`/`order:`) + `#Predicate` for SwiftUI views vs. `FetchDescriptor`/`context.fetch(_:)` for imperative one-time fetches elsewhere; and relationships/cascade delete (`relationships-and-cascade-delete`) — `@Relationship(deleteRule:)`'s four cases (`.cascade`/`.nullify` default/`.deny`/`.noAction`) and the `inverse:` requirement for referential integrity. CloudKit sync, `SchemaMigrationPlan`/`VersionedSchema` migration, Core Data interop, `#Index`/`#Unique` beyond basic `@Attribute(.unique)`, and widget/App-Group container sharing remain out of scope — thirteenth Tier 2 domain complete. `core-data` (Tier 2 — Core Data framework API v1, the older, Objective-C-rooted persistence framework for non-SwiftUI/legacy codebases, now closing the persistence seam left open by `swiftdata`: model definition (`model-definition`) — `NSManagedObject` subclassing, the three `.xcdatamodeld` Codegen modes (Class Definition/Category+Extension/Manual-None) and when to use each, `@NSManaged` properties, and the corrected finding that Xcode's generated code adds only `Identifiable` automatically, never `Codable`/`Hashable`; persistent container setup (`persistent-container-setup`) — `NSPersistentContainer(name:)`, `loadPersistentStores(completionHandler:)`, `viewContext`, `NSPersistentStoreDescription`/`persistentStoreDescriptions`, and injecting the context via `.environment(\.managedObjectContext, ...)`; managed object context CRUD (`managed-object-context-crud`) — `NSEntityDescription.insertNewObject(forEntityName:into:)`/generated `init(context:)`, `delete(_:)`, `save()` and checking `hasChanges`, `perform(_:)`/`performAndWait(_:)` thread confinement, and a basic parent-child context relationship via `parent`; fetching (`fetching-with-nsfetchrequest`) — `NSFetchRequest<T>`/`NSPredicate`/`NSSortDescriptor`/`context.fetch(_:)` and SwiftUI's `@FetchRequest`; and relationships/delete rules (`relationships-and-delete-rules`) — `NSDeleteRule`'s four cases (`.cascadeDeleteRule`/`.nullifyDeleteRule` default/`.denyDeleteRule`/`.noActionDeleteRule`) and the required `inverseRelationship`, with the corrected finding that Core Data's delete-rule case names carry a `DeleteRule` suffix distinct from SwiftData's shorter `.cascade`/`.nullify`/`.deny`/`.noAction` spellings for the same four concepts. `NSPersistentCloudKitContainer`/CloudKit sync, lightweight/mapping-model migration (`NSMigrationManager`, versioned/mapping models), `NSFetchedResultsController`, multi-context concurrency beyond a basic parent-child relationship, and Core Data↔SwiftData interop remain out of scope — fourteenth Tier 2 domain complete, resolving the `core-data`/`swiftdata` boundary this document had left open (see Cross-Domain Notes).

## Tier 1 — Must-Have

| Domain | Slug | Initial Scope | Owns |
|---|---|---|---|
| Apple Style Guide | style-guide | Terminology, capitalization, punctuation, writing style | UI copy wording, capitalization rules, punctuation, inclusive writing |
| Human Interface Guidelines | human-interface-guidelines | Foundations (iOS/iPadOS): layout, color, typography, app icons, images, inclusion, accessibility-design, dark mode, materials, motion, icons, branding, privacy-design, SF Symbols usage, RTL. Plus curated Patterns/Components/Inputs v1: onboarding, searching, settings, notifications, feedback, undo/redo (Patterns); lists and tables, buttons, sheets, alerts, action sheets, navigation bars, tab bars, pickers, toggles, text fields, menus (Components); touchscreen gestures (Inputs). Remaining Patterns/Components/Inputs topics deferred — see Cross-Domain Notes. | Foundations-layer visual/UX design guidance for iOS/iPadOS (layout, color, typography, app icons, images, inclusive-design content, materials, motion, iconography, branding, accessibility-design, privacy-design, RTL), plus a curated Patterns/Components/Inputs v1 design-guidance subset |
| App Store Review Guidelines | app-store-review-guidelines | 2.1 App Completeness, 2.3 Accurate Metadata, 3.1.1 In-App Purchase, 4.2 Minimum Functionality, 4.3 Spam/Duplicate, 5.1.1/5.1.2 Privacy (data collection & sharing). Safety, most of Legal, and Design 4.0 (owned by human-interface-guidelines) out of scope — see Cross-Domain Notes. | App Store submission, metadata, and distribution compliance rules |
| SwiftUI | swiftui | Views (composition, identity, modifier order), Navigation (NavigationStack, NavigationSplitView), Layout (stacks/spacing, safe area, lazy grids, GeometryReader), State management (@State/@Binding, @Observable, @Environment). Plus Animation (implicit/explicit, timing curves, transitions, matchedGeometryEffect, Animatable, PhaseAnimator/KeyframeAnimator) and Gestures (tap/long-press, drag, magnification/rotation, gesture composition, GestureState). Targets iOS 17+ conventions; legacy ObservableObject/NavigationView, previews, and custom Layout protocol conformances out of scope — see Cross-Domain Notes. | SwiftUI view, navigation, layout, state-management, animation, and gesture implementation conventions |
| UIKit | uikit | Programmatic screen-scaffolding v1: view controller lifecycle and composition, Auto Layout (constraints, stack views, safe area), navigation (UINavigationController, UITabBarController, modal presentation), diffable table/collection views. No Storyboard/XIB, no classic data source pattern. Accessibility APIs owned by `accessibility` — see Cross-Domain Notes. | UIKit programmatic screen-scaffolding implementation conventions (view controllers, Auto Layout, navigation, diffable lists/grids) |
| Accessibility | accessibility | SwiftUI + UIKit accessibility API implementation: labels/traits/value/hint, custom actions, element grouping, VoiceOver navigation order, Dynamic Type API, reduce-motion/transparency/increase-contrast, keyboard access & focus, hidden/decorative elements, accessibility audits. Design-level accessibility guidance owned by human-interface-guidelines — see Cross-Domain Notes. | SwiftUI + UIKit accessibility API implementation, VoiceOver/Dynamic Type/reduce-motion support, and accessibility audit conventions |
| SF Symbols | sf-symbols | SF Symbols API implementation v1: symbol basics (Image(systemName:)/UIImage(systemName:)), rendering modes (monochrome/hierarchical/palette/multicolor), symbol variants (fill/circle/square/slash), variable value symbols, weight/scale configuration, color/tinting mechanics, custom symbol usage, UIKit SymbolConfiguration. Symbol effects/animations and Symbol Composer authoring deferred — see Cross-Domain Notes. | SF Symbols API implementation across SwiftUI and UIKit (rendering, variants, variable value, weight/scale, color/tinting, custom symbol usage, UIKit SymbolConfiguration) |
| Xcode | xcode | Xcode GUI/project-file v1: build configurations & .xcconfig files, schemes/targets, automatic and manual code signing, entitlements/capabilities, archive process, export options. No xcodebuild CLI, no CI signing automation, no Swift Package Manager build configuration. | Xcode project-configuration implementation conventions (build settings, xcconfig, schemes, signing, entitlements, archive/export) |
| Networking | networking | Async/await URLSession v1: request construction, data fetching, Codable decoding, HTTP error handling, task cancellation, session configuration, App Transport Security, authenticated requests. No completion-handler APIs, no Combine, no URLSessionDelegate-based background/progress/TLS handling. Sign-in UX owned by `authentication` — see Cross-Domain Notes. | URLSession async/await implementation conventions (requests, decoding, error handling, cancellation, session configuration, ATS, authenticated requests) |
| Local Authentication | local-authentication | iOS/iPadOS LocalAuthentication framework API v1: availability and biometry-type detection, policy evaluation (biometrics-only vs. biometrics-or-passcode), reason strings & NSFaceIDUsageDescription, LAError handling, LAContext lifecycle, Keychain-biometric binding (SecAccessControl), fallback UX. No macOS/watchOS-specific behavior, no general Keychain storage. | Biometric and device-passcode authentication API implementation, including the Keychain-biometric binding seam |
| App Tracking Transparency | app-tracking-transparency | iOS/iPadOS AppTrackingTransparency + AdSupport framework API v1: authorization-request mechanics (one-time-only semantics, .active-state requirement), authorization status handling, IDFA access (zeroed-UUID fallback, don't-cache rule), NSUserTrackingUsageDescription. No tvOS-specific behavior, no SKAdNetwork, no AdServices attribution. | Tracking-permission API implementation and IDFA access conventions |

## Tier 2

| Domain | Slug | Initial Scope | Owns |
|---|---|---|---|
| App Intents | app-intents | App Intents framework API implementation v1: app intent declaration and parameters (`app-intent-declaration-and-parameters`) — `AppIntent` conformance (`title`/`description`/`perform()`), the `@Parameter` property wrapper (`IntentParameter`), `AppEnum`-conforming custom enums, `parameterSummary`; app entities and queries (`app-entities-and-queries`) — `AppEntity` conformance, `EntityQuery`/`EntityStringQuery`, `DisplayRepresentation`, `defaultQuery`, `suggestedEntities()`; App Shortcuts and Siri phrases (`app-shortcuts-and-siri-phrases`) — `AppShortcutsProvider`'s `appShortcuts`, the `AppShortcut` struct, `\(.applicationName)` phrase-authoring rules, `updateAppShortcutParameters()`; intent results and widget hookup (`intent-results-and-widget-hookup`) — `IntentResult`/`ReturnsValue`/`ProvidesDialog`/`OpensIntent`, and authoring an intent a widget will later reference. No legacy `SiriKit`, no custom Siri vocabulary beyond basic phrase authoring, no Interactive Snippets, no Spotlight indexing of entities, no `LiveActivityIntent` specifics, no wiring an already-authored intent into a widget (owned by `widgetkit`) — see Cross-Domain Notes. | App Intents framework API implementation (declaration/parameters, entities/queries, App Shortcuts/Siri phrases, intent results) — intent authoring only, not widget-side wiring or legacy SiriKit |
| WidgetKit | widgetkit | WidgetKit framework API implementation v1: widget declaration and families (`widget-declaration-and-families`) — `Widget`/`WidgetConfiguration` conformance, `StaticConfiguration`/`AppIntentConfiguration`, `WidgetBundle`, `supportedFamilies`/`widgetFamily`, `containerBackground(for:)`, `kind`-string stability; timeline provider and entries (`timeline-provider-and-entries`) — `TimelineProvider`'s `placeholder(in:)`/`getSnapshot(in:completion:)`/`getTimeline(in:completion:)`, `TimelineEntry`, `Timeline`, `TimelineReloadPolicy`; widget interactivity and deep links (`widget-interactivity-and-deep-links`) — `widgetURL(_:)`, `Link`, `Button(intent:)`/`Toggle(_:isOn:intent:)` wiring an already-authored `AppIntent`; timeline reloading and refresh budget (`timeline-reloading-and-refresh-budget`) — `WidgetCenter.reloadTimelines`/`reloadAllTimelines`, best-effort system-managed refresh budget. No Live Activities/`ActivityKit`, no watchOS complications as a distinct surface, no Control Widgets (iOS 18 `Controls`/`ControlWidget`), no StandBy-specific layout, no `AppIntent` authoring (deferred to future `app-intents`), no background-refresh scheduling (deferred to future `backgroundtasks`) — see Cross-Domain Notes. | WidgetKit framework API implementation (declaration/families, timeline provider/entries, interactivity/deep links, timeline reloading/refresh budget) — API mechanics only, not `AppIntent` authoring or background-task scheduling |
| UserNotifications | usernotifications | Client-side UserNotifications + UIKit push-registration API v1: authorization request (`requestAuthorization`, `UNAuthorizationOptions`, `getNotificationSettings`), local notification scheduling (`UNMutableNotificationContent`, `UNNotificationRequest`, Time Interval + Calendar triggers), remote push registration (`registerForRemoteNotifications` and its app-delegate callbacks — device-token handling only, not APNs server-side payload construction), delegate handling (`UNUserNotificationCenterDelegate`: foreground presentation, response handling), actions/categories (`UNNotificationAction`, `UNTextInputNotificationAction`, `UNNotificationCategory`), and managing pending/delivered requests plus badge count. No rich-media extensions, critical alerts, location-based triggers, APNs server-side payloads, ActivityKit/Live Activities, or watchOS. Notification *design* owned by `human-interface-guidelines` — see Cross-Domain Notes. | UserNotifications framework API implementation (authorization, local scheduling, remote push registration, delegate handling, actions/categories, pending/delivered/badge management) |
| BackgroundTasks | backgroundtasks | BackgroundTasks framework API implementation v1: background task registration and scheduling (`background-task-registration-and-scheduling`) — `BGTaskScheduler.shared.register(forTaskWithIdentifier:using:launchHandler:)`, `BGTaskSchedulerPermittedIdentifiers`, `BGAppRefreshTaskRequest`/`BGProcessingTaskRequest`, `submit(_:)`, `earliestBeginDate`; task execution and expiration handling (`task-execution-and-expiration-handling`) — `BGTask`/`BGAppRefreshTask`/`BGProcessingTask`, `expirationHandler`, `setTaskCompleted(success:)`, re-submission for recurrence; processing task constraints and conditions (`processing-task-constraints-and-conditions`) — `requiresNetworkConnectivity`/`requiresExternalPower`, choosing between request types; background refresh and widget timeline hookup (`background-refresh-and-widget-timeline-hookup`) — scheduling a `BGAppRefreshTaskRequest` to feed widget data, hand-off to `widgetkit`'s reload call. No `BGContinuedProcessingTask`, no legacy Background Fetch, no unrelated background modes, no `URLSession` background transfer (owned by `networking`) — see Cross-Domain Notes. | BackgroundTasks framework API implementation (registration/scheduling, execution/expiration, processing constraints, widget-refresh hookup) — API mechanics only, not `URLSession` background transfer |
| Foundation | foundation | Curated, highest-usage v1 subset (not exhaustive): date/time formatting (`DateFormatter`, `ISO8601DateFormatter`, `Date.FormatStyle`, `RelativeDateTimeFormatter`, formatter-reuse performance); measurement and unit formatting (`Measurement`, `.converted(to:)`, `MeasurementFormatter`); Codable encoding and custom conformance (`JSONEncoder`, custom `encode(to:)`/`init(from:)`, `CodingKeys`); FileManager app sandbox directories (`Documents`/`Caches`/`Application Support`, safe file I/O, `isExcludedFromBackup`). No general String/Unicode processing, `NotificationCenter`, `Result`, GCD/`DispatchQueue`, Locale/Bundle localization, Combine, or network-response Codable decoding (owned by `networking`) — see Cross-Domain Notes. | Core Swift Foundation data-type and utility usage conventions (curated v1 subset: date/time, measurement/units, Codable encoding, file-system sandbox directories) |
| Localization | localization | Language, terminology | Localization and translation workflow conventions |
| Privacy | privacy | `PrivacyInfo.xcprivacy` privacy manifest file implementation v1: manifest file structure/bundling per product type, per-target/framework/xcframework manifest requirement (`manifest-file-structure-and-scope`); required-reason API categories and `NSPrivacyAccessedAPITypeReasons` justification codes (`required-reason-api-declarations`); `NSPrivacyCollectedDataTypes` entry schema (`collected-data-types-declaration`); `NSPrivacyTracking`/`NSPrivacyTrackingDomains` and the third-party SDK privacy-manifest-and-signature requirement (`tracking-domains-and-third-party-sdk-signatures`). No App Store Connect nutrition-label questionnaire, no permission-request UI design, no Info.plist usage strings, no Keychain/security — see Cross-Domain Notes. | Privacy Manifest file structure, schema, and enum-value correctness (manifest implementation, not review consequence or permission-UI design) |
| AuthenticationServices | authenticationservices | Sign in with Apple API implementation v1: request-and-credential handling (`ASAuthorizationAppleIDProvider().createRequest()`, `requestedScopes`, `ASAuthorizationController`, `ASAuthorizationControllerDelegate`/`ASAuthorizationControllerPresentationContextProviding`, `ASAuthorizationAppleIDCredential`, `ASAuthorizationError`) (`sign-in-with-apple-request-and-credential`); nonce and identity-token verification (SHA256-hashed nonce, raw-nonce/`identityToken`/`authorizationCode` forwarding to a backend) (`nonce-and-identity-token-verification`); credential state and revocation (`getCredentialState(forUserID:)`, `CredentialState`, `credentialRevokedNotification`) (`credential-state-and-revocation`); session persistence and sign-out (the `user` identifier as durable account key) (`session-persistence-and-sign-out`). No Password AutoFill/credential-provider extensions, no Passkeys/WebAuthn, no server-side JWT verification — see Cross-Domain Notes. Absorbs the former separate `sign-in-with-apple` placeholder row (retired, no distinct content). | Sign in with Apple API implementation (request/credential, nonce/identity-token handling, credential-state/revocation, session persistence) — API mechanics only, not sign-in UX/terminology or Keychain CRUD |
| StoreKit | storekit | StoreKit 2 async/await API v1: product loading and purchase initiation (`product-loading-and-purchase`) — `Product.products(for:)`, `product.purchase()`, `Product.PurchaseResult`; transaction verification and entitlements (`transaction-verification-and-entitlements`) — `VerificationResult`, `Transaction.currentEntitlements`, `transaction.finish()`; transaction updates and restoring purchases (`transaction-updates-and-restoring-purchases`) — `Transaction.updates` listener, `AppStore.sync()`; subscription status and renewal info (`subscription-status-and-renewal-info`) — `Product.SubscriptionInfo.Status`/`RenewalInfo`/`renewalState`, subscription-group upgrade/downgrade. No legacy StoreKit 1 (`SKPaymentQueue`/`SKProduct`/`SKPaymentTransaction`), no server-side receipt validation/App Store Server API, no StoreKit Configuration file test setup — see Cross-Domain Notes. | StoreKit 2 API implementation (product loading, purchase, transaction verification, entitlements, restoring purchases, subscription status) — API mechanics only, not App Review compliance rules |
| Core Data | core-data | Core Data framework API implementation v1: model definition (`model-definition`) — `NSManagedObject` subclassing, `.xcdatamodeld` Codegen modes (Class Definition/Category+Extension/Manual-None), `@NSManaged` properties; persistent container setup (`persistent-container-setup`) — `NSPersistentContainer(name:)`, `loadPersistentStores(completionHandler:)`, `viewContext`, `NSPersistentStoreDescription`; managed object context CRUD (`managed-object-context-crud`) — `insertNewObject(forEntityName:into:)`/`init(context:)`, `delete(_:)`, `save()`, `perform(_:)`/`performAndWait(_:)`, basic parent-child `parent` context; fetching (`fetching-with-nsfetchrequest`) — `NSFetchRequest<T>`/`NSPredicate`/`NSSortDescriptor`/`context.fetch(_:)`, SwiftUI `@FetchRequest`; relationships and delete rules (`relationships-and-delete-rules`) — `NSDeleteRule` (`.cascadeDeleteRule`/`.nullifyDeleteRule` default/`.denyDeleteRule`/`.noActionDeleteRule`), `inverseRelationship`. No CloudKit sync, no migration, no `NSFetchedResultsController`, no multi-context concurrency beyond basic parent-child, no SwiftData interop — see Cross-Domain Notes. | Core Data framework API implementation (model definition, container setup, context CRUD, fetching, relationships/delete rules) — API mechanics only, not SwiftData or CloudKit sync |
| SwiftData | swiftdata | SwiftData framework API implementation v1: model definition (`model-definition`) — `@Model` on a class, `@Attribute`/`@Relationship`/`@Transient`, auto-synthesized `PersistentModel`/`Observable` conformance (not `Codable`); model container setup (`model-container-setup`) — `.modelContainer(for:)`/`.modelContainer(_:)`, `ModelConfiguration`, `ModelContainer(for:configurations:)`, `@Environment(\.modelContext)`; model context CRUD (`model-context-crud`) — `insert(_:)`/`delete(_:)`/`save()`, `autosaveEnabled`, `mainContext` vs. secondary context, `undoManager`; querying (`querying-with-query-and-fetchdescriptor`) — `@Query`/`#Predicate` vs. `FetchDescriptor`/`context.fetch(_:)`; relationships and cascade delete (`relationships-and-cascade-delete`) — `@Relationship(deleteRule:)` (`.cascade`/`.nullify`/`.deny`/`.noAction`), `inverse:` requirement. No CloudKit sync, no `SchemaMigrationPlan`/`VersionedSchema` migration, no Core Data interop, no `#Index`/`#Unique` beyond basic `@Attribute(.unique)`, no widget/App-Group container sharing — see Cross-Domain Notes. | SwiftData framework API implementation (model definition, container setup, context CRUD, querying, relationships/delete rules) — API mechanics only, not Core Data or CloudKit sync |
| PassKit | passkit | PassKit framework API implementation v1: pass library querying/adding (`pass-library-and-authorization`) — `PKPassLibrary.isPassLibraryAvailable()`/`containsPass(_:)`/`passes()`/`passes(of:)`/`addPasses(_:withCompletionHandler:)`; `.pkpass`/`pass.json` structure (`pass-content-and-required-fields`) — required keys, style keys, `PassFields`, `barcodes`/`relevantDates`, `PKPass(data:)`; the add-to-Wallet UI flow (`adding-passes-ui`) — `PKAddPassesViewController`/`PKAddPassButton`, `canAddPasses()`; pass updates and the app-vs-server push boundary (`pass-updates-and-push-registration`) — `webServiceURL`/`authenticationToken`, device-registration REST protocol, no PassKit-specific `PKPushType` exists; Apple Pay payment requests (`apple-pay-payment-request`) — `PKPaymentRequest`, `canMakePayments()`, `PayWithApplePayButton`/`PKPaymentButton`; Apple Pay authorization and result handling (`apple-pay-authorization-and-result-handling`) — `PKPaymentAuthorizationControllerDelegate`, `PKPayment`/`PKPaymentToken`, `PKPaymentAuthorizationResult`. No server-side pass signing/certificate management, no `PKAddSecureElementPassViewController`/NFC/secure-element passes, no `PKPassPersonalization`, no Apple Pay server-side merchant validation/token decryption — see Cross-Domain Notes. | PassKit framework API implementation (pass library, pass content, add-to-Wallet UI, pass updates, Apple Pay request/authorization) — API mechanics only, not server-side signing/validation |
| TipKit | tipkit | TipKit framework API implementation v1: tip declaration and content (`tip-declaration-and-content`) — `Tip` protocol conformance on a `struct`, `title`, optional `message`/`image`, `actions` via `Tip.Action`; display rules and event triggers (`display-rules-and-event-triggers`) — `#Rule(_:)` macro over `Tips.Parameter`/`Tips.Event`, AND-combination; tip options and app configuration (`tip-options-and-app-configuration`) — `Tips.configure(_:)`, `datastoreLocation(_:)`/`displayFrequency(_:)`, per-tip `MaxDisplayCount`/`MaxDisplayDuration`/`IgnoresDisplayFrequency`; presenting tips and tip groups (`presenting-tips-and-tip-groups`) — `TipView`/`popoverTip`/`TipUIView`/`TipUIPopoverViewController`, `TipGroup`, `invalidate(reason:)`. No custom `TipViewStyle` authoring, no watchOS-specific presentation, no `cloudKitContainer(_:)` cross-device sync (real capability, deliberately excluded). | TipKit framework API implementation (declaration/content, display rules/event triggers, options/app configuration, presentation/tip groups) — API mechanics only, not custom `TipViewStyle` authoring |
| Combine | combine | Reactive data flow | Publisher/subscriber usage conventions, SwiftUI interop |
| EventKit | eventkit | EventKit framework API implementation v1: authorization and access levels (`authorization-and-access-levels`) — `EKAuthorizationStatus`, iOS 17+ `requestFullAccessToEvents(completion:)`/`requestWriteOnlyAccessToEvents(completion:)`/`requestFullAccessToReminders(completion:)` vs. legacy `requestAccess(to:completion:)`, matching Info.plist usage-description keys; event CRUD and fetch predicates (`event-crud-and-fetch-predicates`) — `EKEvent`, `save(_:span:commit:)`/`remove(_:span:commit:)`, `predicateForEvents(withStart:end:calendars:)` + `events(matching:)`; reminder CRUD and fetch (`reminder-crud-and-fetch`) — `EKReminder`, `save(_:commit:)`/`remove(_:commit:)`, `predicateForReminders(in:)` + async-only `fetchReminders(matching:completion:)`; recurrence rules and EventKitUI hand-off (`recurrence-rules-and-eventkitui-handoff`) — `EKRecurrenceRule`/`EKRecurrenceEnd`, `EKEventEditViewController`/`EKEventViewController` vs. custom UI. No `EKSource`/multi-account calendar-source management, no CalDAV/Exchange specifics, no `EKEventStoreChanged` live-sync, no EventKit inside a widget extension (owned by `widgetkit`). | EventKit framework API implementation (authorization/access levels, event CRUD/fetch, reminder CRUD/fetch, recurrence rules/EventKitUI hand-off) — API mechanics only, not multi-account source management |
| Testing | testing | XCTest, Swift Testing, UI testing | XCTest, Swift Testing, and UI testing conventions |
| Security | security | General (non-biometric-bound) Keychain Services API v1: Keychain item CRUD (`keychain-item-crud`) — `SecItemAdd`/`SecItemCopyMatching`/`SecItemUpdate`/`SecItemDelete` for `kSecClassGenericPassword`/`kSecClassInternetPassword`, `OSStatus`/`errSec*` handling; Keychain accessibility levels (`keychain-accessibility-levels`) — the `kSecAttrAccessible*` family; Keychain access groups and sharing (`keychain-access-groups-and-sharing`) — `kSecAttrAccessGroup`, Keychain Sharing capability, team-ID prefix convention; storing structured/`Codable` data (`storing-structured-data-in-keychain`) — `kSecValueData` encoding/decoding. No biometric-bound access control (`SecAccessControl`), no AutoFill/credential-provider extensions, no iCloud Keychain sync, no certificate/key/identity item classes — see Cross-Domain Notes. | Keychain Services API implementation (item CRUD, accessibility levels, access-group sharing, structured-data storage) for general (non-biometric-bound) generic/internet password items |

## Tier 3

| Domain | Slug | Initial Scope | Owns |
|---|---|---|---|
| AVFoundation | avfoundation | Audio/video capture & playback | Audio/video capture and playback implementation |
| Vision | vision | Image analysis | Image analysis API usage |
| Core ML | core-ml | On-device ML | On-device ML model integration conventions |
| CloudKit | cloudkit | CloudKit | CloudKit sync and record management conventions |
| HealthKit | healthkit | Health data | Health data access and terminology |
| MapKit | mapkit | Maps | Map display and interaction conventions |
| Photos | photos | Photo library access | Photo library access and permission conventions |
| Core Location | core-location | Location services | Location services access and permission conventions |
| Apple Ads | apple-ads | Ad attribution: AdAttributionKit, SKAdNetwork | Ad-campaign attribution and measurement implementation |
| HomeKit | homekit | Smart-home accessory control | HomeKit accessory and automation implementation |
| CarPlay | carplay | In-car app UI | CarPlay app template and UI conventions |
| ARKit | arkit | AR session/tracking | AR session setup and tracking conventions |
| RealityKit | realitykit | 3D/AR rendering | RealityKit entity/scene rendering conventions (see Cross-Domain Notes) |
| GameKit | gamekit | Leaderboards, matchmaking | Game Center leaderboard, achievement, and matchmaking conventions |
| MusicKit | musickit | Apple Music playback/catalog | Apple Music catalog and playback integration |
| Contacts | contacts | Contacts access | Contacts framework access and permission conventions |
| MessageUI | messageui | Mail/SMS compose sheets | Mail and message compose-sheet implementation |
| Speech | speech | Speech recognition | Speech-to-text implementation conventions |
| Natural Language | naturallanguage | Text analysis | On-device text analysis (tokenization, language ID, sentiment) conventions |

## Existing / Unscheduled Domains

Mapped before this Tier list existed. No Tier assigned yet — resolve when reached.

| Domain | Status | Initial Scope | Owns |
|---|---|---|---|
| authentication | Active (Phase 5, in progress) | Sign in, identity, sessions — see Cross-Domain Notes | Sign-in, identity, and session implementation routing (see Cross-Domain Notes) |

## Cross-Domain Notes

- `authentication`, `authenticationservices` (Tier 2, complete), and the former `sign-in-with-apple` placeholder resolved: `authenticationservices` absorbs `sign-in-with-apple` outright (same framework, same feature — no distinct content existed for a separate row), and is a clean handoff with `authentication`, not an angle-split. `knowledge.authentication.authentication`'s Excluded list already named "Sign in with Apple implementation" before `authenticationservices` was built; `authentication` continues to own sign-in terminology, entry points, and user-facing flow decisions (including the "Sign in with Apple" button's terminology, per `style-guide`'s capitalization rule), while `authenticationservices` owns the `ASAuthorizationAppleIDProvider`/`ASAuthorizationController` API surface. No content is duplicated between the two domains.
- `human-interface-guidelines` and `sf-symbols` were previously merged with `style-guide` under a single `design` domain. Split per ../../rfcs/0001-style-guide-domain-and-domain-roadmap.md ([[0001-style-guide-domain-and-domain-roadmap]]).
- `core-data` and `swiftdata` (both Tier 2, complete) overlap (both persistence), now resolved: both are built as separate, non-overlapping frameworks with separate APIs — a clean split, not an angle-split, since they solve the same problem with entirely distinct API surfaces (`NSManagedObject`/`NSPersistentContainer`/`NSManagedObjectContext` vs. `@Model`/`ModelContainer`/`ModelContext`). Neither domain's KCs reference or restate the other's content. No shared "which framework to recommend" note was added — that judgment call is left to whoever is building the app, not encoded as a rule here.
- `arkit` and `realitykit` overlap (AR/3D rendering, RealityKit often layers on ARKit sessions). Boundary not yet resolved — decide when either is reached.
- `swiftui` and `human-interface-guidelines` overlap on layout (`swiftui`'s `stacks-and-spacing`/`safe-area`/`lazy-grids` vs. `human-interface-guidelines`'s `layout.md`) and, since `swiftui`'s Animation/Gestures v1, on motion and touch interaction too (`swiftui`'s `animation-modifiers.md`/`phase-and-keyframe-animators.md` vs. `human-interface-guidelines`'s `motion.md`; `swiftui`'s gesture KCs vs. `human-interface-guidelines`'s `touchscreen-gestures.md`, whose own Excluded section already names this handoff). Resolved via angle-split: `swiftui`'s angle is code-implementation (which API, correct syntax, performance), `human-interface-guidelines`'s angle is visual-design (spacing/alignment, when/why to animate, which gesture to use — a design decision). Same pattern as the `app-store-review-guidelines` privacy KCs vs. `privacy`.
- `swiftui` and `combine` (Tier 2, unbuilt) overlap on state management — `combine`'s Owns line already covers "SwiftUI interop." Resolved via angle-split: `swiftui`'s `observable-macro.md` teaches `@Observable` as the modern, non-Combine replacement for `ObservableObject`; `combine`'s angle (when built) is Combine-specific publisher/subscriber patterns. Boundary confirmed when `combine` is reached.
- `human-interface-guidelines` (`accessibility` Foundations topic) and `accessibility` overlap: HIG's angle is design guidance (Dynamic Type requirement, contrast ratio, not conveying state by color alone, gesture alternatives — the *what* and *why*), `accessibility`'s angle is API implementation (the *how* — `accessibilityLabel`, `accessibilityTraits`, `@ScaledMetric`, `accessibilityReduceMotion`, etc.). Resolved via angle-split, the same pattern as the `swiftui` vs. `human-interface-guidelines` layout overlap.
- `human-interface-guidelines` (`privacy` Foundations topic) and `privacy` (Tier 2, complete) overlap: HIG owns permission-request UI/consent-flow design, `privacy` owns Privacy Manifest file structure/schema implementation. Resolved via angle-split, same pattern as `accessibility` vs. `human-interface-guidelines`. `privacy`'s KCs cross-reference `knowledge.human-interface-guidelines.privacy` via `related:` rather than restating its Rules; `human-interface-guidelines/privacy.md`'s own Excluded section already named this boundary before `privacy` was built.
- `human-interface-guidelines` (`sf-symbols` Foundations topic) and `sf-symbols` overlap: HIG owns symbol selection/composition as a design decision (which symbol, which color, fill vs. outline as a design choice), `sf-symbols` owns API implementation (rendering modes, variants, variable value, weight/scale, color/tinting mechanics, custom symbol usage, UIKit SymbolConfiguration). Resolved via angle-split — `sf-symbols` KCs cross-reference `human-interface-guidelines`'s `sf-symbols.md` via `related:` rather than restating its Rules.
- `sf-symbols` and `uikit` overlap: `uikit` KCs may display symbols inside their examples but don't own symbol-rendering rules — a future `uikit` KC needing symbol-specific guidance should cross-reference `sf-symbols` via `related:` rather than duplicating rendering-mode/weight/scale content. No existing `uikit` KC required updating for this domain's launch.
- `sf-symbols` and `swiftui` overlap: same pattern as `sf-symbols` vs. `uikit` — `swiftui` owns view composition, not symbol rendering. No existing `swiftui` KC required updating for this domain's launch.
- `app-store-review-guidelines` (`privacy-manifest`/`privacy-nutrition-label` topics) and `privacy` (Tier 2, complete) overlap: `app-store-review-guidelines`'s angle is review consequence (submission gets rejected if the manifest/label is missing or inaccurate), `privacy`'s angle is correct implementation (exact schema, enum values, and reason codes to write the manifest right the first time). Resolved via angle-split — `privacy`'s KCs cross-reference `knowledge.app-store-review-guidelines.privacy-manifest`/`privacy-nutrition-label` via `related:` rather than restating their Rules. `privacy`'s `collected-data-types-declaration.md` and `app-store-review-guidelines`'s `privacy-nutrition-label.md` are a clean handoff specifically (manifest-file declaration vs. the separate App Store Connect web-form questionnaire), not an angle-split, since they're two distinct disclosure surfaces asking similar substantive questions.
- `accessibility` (`accessibility-audits-testing` topic) and the future `testing` domain (Tier 2, unbuilt) overlap: this domain's angle is accessibility-specific audit APIs (`performAccessibilityAudit`, Accessibility Inspector), `testing`'s future angle is general XCTest/Swift Testing/UI-testing conventions. Boundary not yet resolved — decide when `testing` is built.
- `uikit` and `accessibility` overlap: `accessibility` owns all UIKit accessibility API implementation (labels, traits, value/hint, custom actions, element grouping/order, Dynamic Type, reduce-motion/transparency, focus, hidden/decorative, audits) across both SwiftUI and UIKit; `uikit` owns non-accessibility screen-scaffolding APIs (lifecycle, layout, navigation, lists/grids). Resolved via angle-split — `uikit` KCs cross-reference `accessibility` KCs via `related:` rather than restating Rules.
- `uikit` and `swiftui` overlap: both cover screen-building but on separate API surfaces (imperative vs. declarative); neither depends on the other for v1. The interop boundary (`UIHostingController`/`UIViewRepresentable`) is future scope for whichever domain builds it — not yet assigned.
- `uikit` and `human-interface-guidelines` overlap: HIG owns design guidance (when to use a tab bar vs. navigation stack, list vs. grid layout choice, modal vs. push presentation), `uikit` owns API implementation (the *how*). Same angle-split pattern as `accessibility` vs. `human-interface-guidelines`.
- `networking` and `authentication` do not overlap — this is a clean handoff, not an angle-split. `authentication`'s own Knowledge Contract (`knowledge.authentication.authentication`) explicitly excludes "Authentication networking" and "Backend architecture" from its scope; `networking`'s `authenticated-requests` topic fills exactly that gap (attaching credentials to a request, 401 refresh-and-retry), while `authentication` continues to own sign-in UX, terminology, and entry points. No content is duplicated between the two domains.
- `local-authentication` and `authentication` do not overlap — this is a clean handoff, not an angle-split, same pattern as `networking`/`authentication`. `knowledge.authentication.authentication`'s Excluded list omits biometrics entirely (its exclusions are StoreKit authentication, passkeys implementation, Sign in with Apple implementation, authentication networking, backend architecture); `authentication` owns sign-in terminology, entry points, and user-facing flow decisions, while `local-authentication` owns the LocalAuthentication framework API surface reached once the decision to use biometrics has already been made. No content is duplicated between the two domains.
- `app-tracking-transparency` and `human-interface-guidelines` overlap: `knowledge.human-interface-guidelines.privacy` already covers "Tracking-permission-alert integrity rules" (its Rules 3–4: custom pre-permission screen constraints, no deceptive screens before the system tracking alert) as a design/UX topic. Resolved via angle-split — `human-interface-guidelines` keeps the design/UX layer (whether/how to show a custom pre-permission screen, its button/copy constraints, anti-deception rule), `app-tracking-transparency` owns the API layer (the `requestTrackingAuthorization` call mechanics, the status enum, IDFA access). `app-tracking-transparency`'s `authorization-request.md` cross-references `knowledge.human-interface-guidelines.privacy` via `related:` rather than restating its Rules, same cross-domain `related:` pattern already used by `sf-symbols` KCs referencing `human-interface-guidelines`.
- `foundation` (`measurement-and-unit-formatting` topic) and `style-guide` (`units-of-measure` topic) overlap: `foundation` owns producing the locale-correct, unit-converted value/string via `Measurement`/`MeasurementFormatter` API calls, `style-guide` owns the resulting text's copy-formatting rules (spelling out vs. abbreviating, spacing, hyphenation, capitalization) once a unit already appears in UI copy. Resolved via angle-split, same pattern as `accessibility` vs. `human-interface-guidelines`. `units-of-measure.md`'s own Excluded section already named "Locale-specific unit conversion" and "Date and time formatting" as not its concern before `foundation` was built.
- `foundation` (`codable-encoding-and-custom-conformance` topic) and `networking` (`codable-decoding` topic) do not overlap — this is a clean handoff, not an angle-split. `networking`'s `codable-decoding.md` owns `JSONDecoder`/`DecodingError` handling for fetched network response `Data` specifically; `foundation`'s KC owns `JSONEncoder` and custom `encode(to:)`/`init(from:)` conformance mechanics that apply regardless of where the JSON goes or came from. No content is duplicated between the two domains.
- `app-tracking-transparency` and `app-store-review-guidelines` (`privacy-nutrition-label` topic) do not overlap — this is a clean handoff. `privacy-nutrition-label.md`'s Rule 3 already states tracking-use marking "additionally requires App Tracking Transparency permission" without describing the API itself; `app-tracking-transparency` is the implementation that KC points at. No content is duplicated between the two domains.
- `app-tracking-transparency` and `app-store-review-guidelines` (`permission-usage-strings` topic) do not overlap — this is a clean handoff, same pattern as `local-authentication`'s `NSFaceIDUsageDescription` KC vs. the same `permission-usage-strings.md`. That KC's own examples (`NSCameraUsageDescription`, `NSLocationWhenInUseUsageDescription`) never name `NSUserTrackingUsageDescription`; `app-tracking-transparency`'s `usage-string-and-info-plist.md` owns that key's wording specifically. No content is duplicated between the two domains.
- `human-interface-guidelines` (`notifications` Patterns topic) and `usernotifications` (Tier 2, complete) overlap: `human-interface-guidelines` owns notification *design* (content structure, when/how to request permission, foreground handling, action design, badging), `usernotifications` owns `UNUserNotificationCenter`/`UNAuthorizationOptions` API implementation. Resolved via angle-split, same pattern as `accessibility` vs. `human-interface-guidelines`. Every `usernotifications` Knowledge Contract whose topic overlaps (delegate handling, actions/categories, managing pending/delivered/badge) cross-references `knowledge.human-interface-guidelines.notifications` via `related:` rather than restating its Rules.
- `storekit` (Tier 2, complete) and `app-store-review-guidelines` (`digital-goods-iap`/`restore-purchases` topics) do not overlap — this is a clean handoff, same pattern as `local-authentication` vs. `security`. `app-store-review-guidelines` owns the review-compliance angle (the requirement to use IAP to unlock content, prohibited alternative unlock mechanisms, loot-box odds disclosure, the requirement to implement a restore mechanism, the non-expiration rule for purchased currencies); `storekit` owns the StoreKit 2 API implementation angle only (`Product.products(for:)`/`purchase()`, `VerificationResult`/`Transaction.currentEntitlements`/`finish()`, the `Transaction.updates` listener and `AppStore.sync()` mechanics, subscription status/renewal info). `storekit`'s `transaction-updates-and-restoring-purchases.md` cross-references `knowledge.app-store-review-guidelines.restore-purchases` via `related:` rather than restating why a restore mechanism is required. No content is duplicated between the two domains.
- `authenticationservices` (Tier 2, complete) and `security` (Tier 2, complete) do not overlap — this is a clean handoff, same pattern as `local-authentication` vs. `security`. `authenticationservices`'s `session-persistence-and-sign-out.md` owns what to persist (the `user` identifier, not `email` or the raw `identityToken`/`authorizationCode`) and cross-references `knowledge.security.keychain-item-crud` via `related:` for the storage mechanics itself, rather than restating Keychain CRUD. No content is duplicated between the two domains.
- `widgetkit` (Tier 2, complete) has no overlap with any existing domain: no `swiftui`/`human-interface-guidelines` content on widget-specific design exists to hand off to or angle-split against. `widgetkit` proactively deferred two seams to domains not yet built: `AppIntent` authoring itself (parameters, `perform()`, entities) to the future `app-intents` domain, and background-refresh scheduling mechanics (e.g. `BGAppRefreshTask`) to the future `backgroundtasks` domain — both are now resolved. `app-intents` (Tier 2, complete) is a clean handoff with `widgetkit`, not an angle-split: `widgetkit`'s `widget-interactivity-and-deep-links.md` continues to own wiring an already-authored `AppIntent` into a widget's `Button(intent:)`/`Toggle(_:isOn:intent:)`; `app-intents`'s `intent-results-and-widget-hookup.md` owns authoring that intent's `perform()`, parameters, and result. `backgroundtasks` (Tier 2, complete) is likewise a clean handoff: `backgroundtasks`'s `background-refresh-and-widget-timeline-hookup.md` owns registering, submitting, and running the `BGAppRefreshTaskRequest` that produces fresh widget data; `widgetkit`'s `timeline-reloading-and-refresh-budget.md` continues to own the `reloadTimelines`/`reloadAllTimelines` call site and its refresh-budget reasoning once that data has landed. All three KCs cross-reference their counterparts via `related:`/`Dependencies` rather than restating each other's Rules. No content is duplicated across these domains.
- `app-intents` (Tier 2, complete) supersedes legacy `SiriKit` (donation-based intents, `INIntent`, `NSUserActivity` donation) on current OS versions; no separate `SiriKit` domain is planned unless a legacy-support need is identified — this confirms, rather than changes, the note already on record before `app-intents` was built.
- `local-authentication` (`keychain-biometric-binding` topic) and `security` (Tier 2, complete) do not overlap — this is a clean handoff, not an angle-split. `knowledge.local-authentication.keychain-biometric-binding`'s own Excluded section named this boundary before `security` was built ("General Keychain item storage/retrieval for non-biometric-bound items — future `security` domain"): `local-authentication` owns `SecAccessControlCreateWithFlags`/`kSecAttrAccessControl`/`.biometryCurrentSet`/`.biometryAny`/`kSecUseAuthenticationContext` — the seam where a Keychain item is bound to a biometric prompt — while `security` owns general (non-biometric-bound) Keychain item CRUD, accessibility levels, access-group sharing, and structured-data storage. Every `security` Knowledge Contract cross-references `knowledge.local-authentication.keychain-biometric-binding` via `related:` rather than restating its Rules. No content is duplicated between the two domains.

## Artifact Layout

references/apple/<domain>/
knowledge/<domain>/
skills/<domain>/
workflows/<domain>/

## Rules

- Every artifact belongs to exactly one primary domain.
- Cross-domain dependencies must be explicit.
- Skills cannot span unrelated domains.
- Knowledge Contracts remain atomic.

## Validation Checklist

- Domain exists
- Artifact mapped
- Cross-domain dependencies declared
- No duplicate ownership
